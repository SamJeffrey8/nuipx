const Background: React.FC = (): React.ReactNode => {
    return (
        <>
            <div className="pointer-events-none absolute left-0 right-0 top-0 w-full h-screen bg-gradient-to-b from-[#8F00FF] to-black">
            </div>
        </>
    );
};

export default Background;
